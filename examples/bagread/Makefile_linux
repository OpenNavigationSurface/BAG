#############################################################################
# Makefile for building: ../bin/$(HOSTMACHINE)/bagread
# Generated by qmake (1.07a) (Qt 3.3.4) on: Mon Feb  6 23:09:27 2006
# Project:  bagread.pro
# Template: app
# Command: $(QMAKE) -o Makefile bagread.pro
#############################################################################

####### Compiler, tools and options

CC       = gcc
CXX      = g++
LEX      = flex
YACC     = yacc
CFLAGS   = -pipe ${IVSCOPTS} -w -O2 -pipe -m32 -march=i386  -DBEE_STATIC
CXXFLAGS = -pipe ${IVSCCOPTS2} -w -O2 -pipe -m32 -march=i386 -DBEE_STATIC
LEXFLAGS = 
YACCFLAGS= -d
INCPATH  = -I../../extlibs/mkspecs/linux-g++ -I. -I../../api -I../../api/crypto -I../../extlibs/hdf5-linux/include -I../../extlibs/geotrans -I../../extlibs/beecrypt -I../../extlibs
LINK     = g++
LFLAGS   = 
LIBS     = $(SUBLIBS) -L../../libsrc/lib/$(HOSTMACHINE) -L../../extlibs/lib/$(HOSTMACHINE) -L../../lib -lbag -lhdf5 -lxerces-c -lgeotrans -lbeecrypt -lszip -lzlib -lpthread ${IVSEXTRAUNIXLIBS}
AR       = ar cqs
RANLIB   = 
MOC      = $(QTDIR)/bin/moc
UIC      = $(QTDIR)/bin/uic
QMAKE    = qmake
TAR      = tar -cf
GZIP     = gzip -9f
COPY     = cp -f
COPY_FILE= $(COPY)
COPY_DIR = $(COPY) -r
INSTALL_FILE= $(COPY_FILE)
INSTALL_DIR = $(COPY_DIR)
DEL_FILE = rm -f
SYMLINK  = ln -sf
DEL_DIR  = rmdir
MOVE     = mv -f
CHK_DIR_EXISTS= test -d
MKDIR    = mkdir -p

####### Output directory

OBJECTS_DIR = .obj/$(HOSTMACHINE)/

####### Files

HEADERS = 
SOURCES = bagread.c
OBJECTS = .obj/$(HOSTMACHINE)/bagread.o
FORMS = 
UICDECLS = 
UICIMPLS = 
SRCMOC   = 
OBJMOC = 
DIST	   = ../libincludes.pro \
		../appmaster.pro \
		bagread.pro
QMAKE_TARGET = bagread
DESTDIR  = ../bin/$(HOSTMACHINE)/
TARGET   = ../bin/$(HOSTMACHINE)/bagread

first: all
####### Implicit rules

.SUFFIXES: .c .o .cpp .cc .cxx .C

.cpp.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o $@ $<

.cc.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o $@ $<

.cxx.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o $@ $<

.C.o:
	$(CXX) -c $(CXXFLAGS) $(INCPATH) -o $@ $<

.c.o:
	$(CC) -c $(CFLAGS) $(INCPATH) -o $@ $<

####### Build rules

all: Makefile $(TARGET)

$(TARGET):  $(UICDECLS) $(OBJECTS) $(OBJMOC)  
	test -d ../bin/$(HOSTMACHINE)/ || mkdir -p ../bin/$(HOSTMACHINE)/
	$(LINK) $(LFLAGS) -o $(TARGET) $(OBJECTS) $(OBJMOC) $(OBJCOMP) $(LIBS)

mocables: $(SRCMOC)
uicables: $(UICDECLS) $(UICIMPLS)

$(MOC): 
	( cd $(QTDIR)/src/moc && $(MAKE) )

Makefile: bagread.pro  ../../extlibs/mkspecs/linux-g++/qmake.conf ../libincludes.pro \
		../appmaster.pro
	$(QMAKE) -o Makefile bagread.pro
qmake: 
	@$(QMAKE) -o Makefile bagread.pro

dist: 
	@mkdir -p .obj/$(HOSTMACHINE)/bagread && $(COPY_FILE) --parents $(SOURCES) $(HEADERS) $(FORMS) $(DIST) .obj/$(HOSTMACHINE)/bagread/ && ( cd `dirname .obj/$(HOSTMACHINE)/bagread` && $(TAR) bagread.tar bagread && $(GZIP) bagread.tar ) && $(MOVE) `dirname .obj/$(HOSTMACHINE)/bagread`/bagread.tar.gz . && $(DEL_FILE) -r .obj/$(HOSTMACHINE)/bagread

mocclean:
uiclean:

yaccclean:
lexclean:
clean:
	-$(DEL_FILE) $(OBJECTS)
	-$(DEL_FILE) *~ core *.core


####### Sub-libraries

distclean: clean
	-$(DEL_FILE) ../bin/$(HOSTMACHINE)/$(TARGET) $(TARGET)


FORCE:

####### Compile

.obj/$(HOSTMACHINE)/bagread.o: bagread.c 
	$(CC) -c $(CFLAGS) $(INCPATH) -o .obj/$(HOSTMACHINE)/bagread.o bagread.c

####### Install

install:  

uninstall:  

